Write a Google Test (GTest) unit test for the following C++ function AFTER it has been refactored from MMDB2 to Gemmi.

## Function under test

Function: `$function_name`
Original MMDB symbols: $symbols_list
$additional_instructions

## API reference (use ONLY what is documented here — do not invent functions, types, or members)
$api_context
$call_sites
$coot_types
$header_include
$file_includes
$probe_reference

## Original MMDB source (to be refactored)
```cpp
$source_code
```

$gtest_style
$oracle_values

## What the test must do

- Read the same PDB file the probe used (see the probe reference above)
  via `gemmi::read_structure_file`. Do NOT construct synthetic gemmi
  structures by hand.
- Walk the gemmi hierarchy (`st.models`, `model.chains`, `chain.residues`,
  `residue.atoms`) to find the same logical object the probe pulled out
  of the mmdb manager.
- Assert the refactored function produces exactly the measured values from
  the oracle section, using `EXPECT_EQ` / `EXPECT_NEAR` / `EXPECT_STREQ`.
- Use `TEST()` or `TEST_F()` macros.

## Hard rules

- The ONLY gemmi headers you need are `#include <gemmi/structure.hpp>` and
  `#include <gemmi/model.hpp>`. Do not invent other gemmi header paths.
- Use ONLY Gemmi symbols and members that genuinely exist. If you are not
  sure something exists, do not use it.
- Do NOT mock anything. Do NOT build a structure by hand.

Output ONLY the test code inside a single ```cpp``` block.
