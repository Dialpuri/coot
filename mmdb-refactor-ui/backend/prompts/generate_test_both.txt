Write TWO Google Test (GTest) unit tests for the following C++ function:
1. An MMDB2 version (tests the current implementation)
2. A Gemmi version (tests the future refactored implementation)

Function: `$function_name`
MMDB symbols used: $symbols_list
$additional_instructions
$api_context
$header_include

## Source code:
```cpp
$source_code
```

$gtest_style
$oracle_values

The MMDB test runs the function on real PDB data and captures its output — this is the oracle.
The Gemmi test loads the same PDB file, calls the refactored function, and asserts its output matches the MMDB oracle.
Do NOT hardcode expected values; derive them by running the MMDB function at test runtime.

Required headers for each variant:
  MMDB test:  #include <gtest/gtest.h>  #include <mmdb2/mmdb_manager.h>
  Gemmi test: #include <gtest/gtest.h>  #include <gemmi/structure.hpp>  #include <gemmi/model.hpp>

Output EXACTLY this format — no other text:

=== MMDB TEST ===
```cpp
// MMDB2 version
TEST(FunctionNameTests, MMDB_Behaviour) {
  // ...
}
```

=== GEMMI TEST ===
```cpp
// Gemmi version
TEST(FunctionNameTests, Gemmi_Behaviour) {
  // ...
}
```
